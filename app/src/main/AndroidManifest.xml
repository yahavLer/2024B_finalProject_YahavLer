<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._2024B_finalProject_YahavLer"
        tools:targetApi="31">
        <activity
            android:name=".Managers.NevigationActivity"
            android:exported="false" />
        <activity
            android:name=".ActivityView.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityView.activity_registration" />
        <activity android:name=".ActivityView.activity_login" />
        <activity android:name=".ActivityView.activity_profile" />
        <activity android:name=".ActivityView.stores_of_club_home_view" />
        <activity android:name=".ActivityView.activity_manage_club" />

    </application>

</manifest>